spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/coder_platform}
    username: ${SPRING_DATASOURCE_USERNAME:coder}
    password: ${SPRING_DATASOURCE_PASSWORD:coder}
